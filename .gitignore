# Environment files
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/

# Node / React
node_modules/
dist/
build/
.vite/

# Logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Database / local files
*.sqlite
*.db

# SentinelX agent local runtime state
agent_state.json
agents/desktop-python/agent_state.json

# Temporary files
tmp/
temp/

# Generated repo pack (repomix)
repomix-output.xml
# Agent/skill tooling (not part of the project)
.agents/
.claude/
AGENTS.md

# Local evidence pack (screenshots contain personal info)
Evidence/

# CI-built .ipa downloads
agents/ios-native/dist/

# AI observability: exported training datasets + trained model artifacts
backend/datasets/
backend/app/ml/artifacts/

# Safe Recovery Orchestration: Ed25519 private signing key (never commit)
backend/.secrets/

# Android (agents/android-native)
agents/android-native/android/.gradle/
agents/android-native/android/app/build/
agents/android-native/android/local.properties
# signing key stays local-only (checklist: never commit keystore)
agents/android-native/android/keystore/
# release APKs are build outputs — published via GitHub Releases, not tracked
agents/android-native/dist/

# Desktop agent installer: compiled .exe is a build output — published via
# GitHub Releases, not tracked (the .iss source script itself is tracked)
agents/desktop-python/installer/dist/

# Screenshot/log evidence packs (may contain hostnames, IPs, other incidental
# local detail) — same rationale as docs/Evidence/ above
agents/android-native/Evidence/
