# ── API ──────────────────────────────────────────────────────────
# Backend base URL — include /api/v1 path prefix
# Default matches local FastAPI dev server (uvicorn app.main:app --reload)
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
