fastapi==0.139.2
uvicorn[standard]==0.34.0
gunicorn==23.0.0
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
email-validator==2.2.0
pwdlib[argon2]==0.2.1
slowapi==0.1.9
httpx==0.28.1
# numpy/scikit-learn/joblib are needed at runtime only to unpickle the trained
# IsolationForest artifact (app/services/isolation_forest_service.py,
# app/services/replay_service.py). pandas/pyarrow are NOT imported anywhere in
# app/** — they belong to the offline training and dataset-export tooling, so
# they now live in requirements-ml.txt and stay out of the production
# container (roughly 200 MB of image saved).
numpy==2.1.2
scikit-learn==1.5.2
joblib==1.4.2
cryptography==50.0.0

# OTLP/HTTP metrics ingestion (POST /v1/metrics). opentelemetry-proto carries
# the official generated protobuf definitions - SentinelX does not hand-roll
# the wire format. googleapis-common-protos provides google.rpc.Status, which
# the OTLP specification uses for error response bodies.
opentelemetry-proto==1.29.0
googleapis-common-protos==1.66.0

# Shared rate-limit counters. `limits` (already present via slowapi) ships
# valkey:// storage schemes but resolves its client lazily, so the RESP client
# has to be installed for that backend to work at all. SentinelX defaults to
# the PostgreSQL backend and needs none of this; it is here so an operator who
# sets RATE_LIMIT_BACKEND=valkey gets a working limiter rather than a
# degraded one. MIT-licensed, and the client Valkey's own docs point at.
redis==5.2.1
