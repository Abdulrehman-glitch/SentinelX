# Copy to keystore.properties (gitignored) and fill in the real values.
# CI alternative: KEYSTORE_STORE_PASSWORD / KEYSTORE_KEY_ALIAS /
# KEYSTORE_KEY_PASSWORD / KEYSTORE_FILE environment variables (GitHub secrets).
storeFile=keystore/sentinelx-release.keystore
storePassword=
keyAlias=sentinelx
keyPassword=
